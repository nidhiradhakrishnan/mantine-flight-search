import { createStyles } from "@mantine/core";
const useStyle = createStyles (()=>({
    bg:{
        background: 'transparent linear-gradient(180deg, #F8E4B1 0%, #F2E8CE 100%) 0% 0% no-repeat padding-box',
    },

}));
export default useStyle;